---
const { href, title, snippet } = Astro.props;
const _isRelativeLink = href.startsWith("/") && !href.endsWith(".pdf");
---

<a
    href={href}
    target={_isRelativeLink ? "_self" : "_blank"}
    class="link-card-link group block rounded-2xl p-6 bg-gradient-to-br from-primary-50 via-accent-50 to-primary-100 border border-neutral-200 shadow-soft transition-all duration-400 ease-out transform hover:scale-[1.03] hover:shadow-medium focus:scale-[1.03] focus:shadow-medium focus:outline-none focus:ring-2 focus:ring-primary-400"
>
    <h2 class="text-2xl font-heading font-bold mb-4 flex justify-between items-center text-text-primary group-hover:text-primary-700 transition-colors duration-400">
        <span class="link-card-title">{title}</span>
        <span class="text-primary-500 group-hover:text-accent-600 transition-colors duration-400">&rarr;</span>
    </h2>
    <p class="text-text-secondary">{snippet}</p>
</a>
