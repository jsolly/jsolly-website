---
const { heading } = Astro.props;
type SubheadingType = {
	text: string;
	slug: string;
};
---

<li class="mb-3">
    <a class="toc-link text-xs" href={"#" + heading.slug}>
        {heading.text}
    </a>
    {
        heading.subheadings.length > 0 && (
            <ul class="ml-4 mt-1">
                {heading.subheadings.map((subheading: SubheadingType) => (
                    <Astro.self heading={subheading} />
                ))}
            </ul>
        )
    }
</li>
